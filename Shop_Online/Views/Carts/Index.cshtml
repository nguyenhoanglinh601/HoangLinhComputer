@model List<Shop_Online.Models.ViewModels.CartItem>
@{
    ViewData["Title"] = "Giỏ hàng - ";
}
<nav aria-label="breadcrumb">
    <ol class="breadcrumb px-lg-5">
        <li class="breadcrumb-item"><a href="/">Home</a></li>
        <li class="breadcrumb-item active" aria-current="page">Giỏ hàng</li>
    </ol>
</nav>
<div class="container-fluid">
    <div class="container" id="cart">
        @if (Model != null && Model.Count() > 0)
        {
            <div class="row  d-lg-flex d-none">
                <div class="col-md-3 align-self-center ">
                </div>
                <div class="col-md-8  align-self-center">
                    <div class="row">
                        <div class="col-lg-6 ">
                            <p class="m-0 text-center">
                                <strong>
                                    Sản phẩm
                                </strong>
                            </p>
                        </div>
                        <div class="col-lg-2 ">
                            <p class="m-0 text-center">
                                <strong>
                                    Giá trị
                                </strong>
                            </p>
                        </div>
                        <div class="col-lg-2 ">
                            <p class="m-0 text-center">
                                <strong>
                                    Số lượng
                                </strong>
                            </p>
                        </div>
                        <div class="col-lg-2 ">
                            <p class="m-0 text-center">
                                <strong>
                                    Thành tiền
                                </strong>
                            </p>
                        </div>
                    </div>
                </div>
                <div class="col-md-1 align-self-center ">

                </div>
            </div>
            foreach (var item in Model)
            {
                <div class="row my-3 py-1 border rounded" id="item_@item.product.ID">
                    <div class="col-md-3 align-self-center order-md-1 order-2 ">
                        <img class="img-fluid thumbnail w-100" src="@item.thumbnail.Image" />
                    </div>
                    <div class="col-md-8 align-self-center order-md-2 order-3 ">
                        <div class="row">
                            <div class="col-lg-6 align-self-center ">
                                <h5 class="text-center text-secondary">
                                    <strong>
                                        @item.product.Product_Name
                                    </strong>
                                </h5>
                            </div>
                            <div class="col-lg-2 col-4 align-self-center ">
                                <p class="m-0 text-center text-secondary">
                                    <strong class="product_price price_single">@item.product.price </strong>
                                </p>
                            </div>
                            <div class="col-lg-2 col-4 align-self-center  d-flex text-secondary">
                                <p class="m-0 pt-1">x&nbsp;</p>
                                <input type="number" class="form-control h-100 pr-0 quantity" min="1" max="@item.product.Amount"
                                       value="1" onchange="updateQuantity(@item.product.ID, @item.product.price)"
                                       id="quantity_@item.product.ID" />
                                <p class="m-0 pt-1 d-lg-none d-flex">&nbsp;=</p>
                            </div>
                            <div class="col-lg-2 col-4 align-self-center ">
                                <p class="m-0 text-danger total text-center">
                                    <strong class="product_price price_total" id="price_@item.product.ID">
                                        @item.product.price
                                    </strong>
                                </p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-1 py-1  d-flex justify-content-end  text-warning order-md-3 order-1">
                        <i class="fas fa-times" style="cursor: pointer" onclick="removeProduct(@item.product.ID)"></i>
                    </div>
                </div>
            }
            <div class="row justify-content-end ">
                <div class="col-lg-3 col-md-5 col-8">
                    <div class="row d-flex justify-content-between">
                        <div class="">
                            <p class="mb-1 font-weight-bold text-secondary">Tạm tính: </p>
                            <p class="mb-1 font-weight-bold text-secondary">Phí vận chuyển: </p>
                            <p class="mb-1 font-weight-bold text-secondary">Phụ phí: </p>
                            <p class="mb-1 font-weight-bold text-secondary">Tổng cộng: </p>
                        </div>
                        <div class="">
                            <p class="mb-1 text-right text-secondary product_price" id="cost"></p>
                            <p class="mb-1 text-right text-secondary">0đ</p>
                            <p class="mb-1 text-right text-secondary">0đ</p>
                            <strong class="mb-1 text-right text-secondary product_price text-danger" id="total"></strong>
                        </div>
                    </div>
                </div>
            </div>
            <div class="row justify-content-end">
                <div class="col-lg-3 col-md-5">
                    <div class="row">
                        @*<a class="btn btn-primary w-100 text-white" data-toggle="modal"
                           data-target="#OrderInfoModal" onclick="getCart()">Tiếp tục đặt hàng</a>*@
                        <button class="btn btn-primary w-100 text-white" onclick="checkLogin()">Tiếp tục đặt hàng</button>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
@*<div class="modal fade" id="OrderInfoModal" tabindex="-1" role="dialog" 
     aria-labelledby="OrderInfoModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="OrderInfoModalLabel">Thông tin đặt hàng</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form asp-controller="orders" asp-action="order" method="post">
                    <div class="form-group">
                        <label for="name">Họ tên khách hàng</label>
                        <input type="text" class="form-control" id="name" name="name" placeholder="Nguyễn Văn A" required>
                    </div>
                    <div class="form-group">
                        <label for="phone_number">Số điện thoại</label>
                        <input type="tel" pattern="[0]{1}[0-9]{9}" class="form-control" id="phone_number" name="phone_number" placeholder="0969076447" required>
                    </div>
                    <div class="form-group">
                        <label for="address">Địa chỉ</label>
                        <textarea class="form-control" id="address" name="address"
                                  placeholder="212/58 Thoại Ngọc Hầu, Phường Phú Thạnh, Quận Tân Phú, HCM" required></textarea>
                    </div>
                    <div class="form-group">
                        <label for="note">Ghi chú</label>
                        <textarea class="form-control" id="note" name="note"
                                  placeholder=""></textarea>
                    </div>
                    <div class="form-check">
                        <input type="hidden" name="isNewAccount" value="false" />
                        <input type="checkbox" class="form-check-input" id="is_new_account" checked/>
                        <label class="form-check-label" for="is_new_account">
                            Tạo tài khoản mới cho tôi (nếu quý khách chưa cho tài khoản tại HoangLinh.vn)
                        </label>
                    </div>
                    <div id="account_info" class="mt-2">
                        <div class="form-group">
                            <label for="birth_day">Ngày sinh</label>
                            <input type="date" class="form-control" 
                                   id="birth_day" name="birthDay">
                        </div>
                        <div class="form-group">
                            <label for="gender">Giới tính</label>
                            <select class="form-control" id="gender">
                                <option value="0"></option>
                                <option value="1">Nam</option>
                                <option value="2">Nữ</option>
                            </select>
                        </div>
                        <p class="text-success">Mật khẩu mặt định là ngày tháng năm sinh của quý khách (viết liền, không bao gồm ký tự)</p>
                    </div>
                    <div class="w-100 text-center">
                        <button type="submit" class="btn btn-primary w-100">Đặt hàng</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>*@
<script type="text/javascript" src="~/js/Cart.js"></script>
@*<script type="text/javascript" src="~/js/general.js"></script>*@
<script>
    displayThumbnail();
    convertPrice();
    caculateBillTotal();
</script>